# --- Web framework ---
fastapi
uvicorn[standard]
python-multipart
pydantic
pydantic-settings
python-dotenv

# --- Database (PostgreSQL: Supabase / Neon) ---
sqlalchemy[asyncio]
asyncpg
alembic
greenlet

# --- AI / LLM ---
groq
tenacity                # retry wrapper around Groq calls

# --- RAG / vector store ---
qdrant-client
fastembed               # ONNX embeddings, no torch -> light enough for free tier

# --- Document ingestion ---
pymupdf                 # fast PDF text extraction
pdfplumber              # table extraction from financial statements
pytesseract             # OCR fallback for scanned PDFs (needs `tesseract-ocr` system pkg)
pdf2image               # rasterize for OCR (needs `poppler-utils` system pkg)
pillow

# --- Website analysis ---
httpx
beautifulsoup4
trafilatura             # clean main-content extraction
lxml

# --- Data / ratios ---
pandas
numpy

# --- Report generation ---
jinja2
weasyprint              # HTML -> PDF (needs pango/cairo system libs)

# --- Storage ---
cloudinary
